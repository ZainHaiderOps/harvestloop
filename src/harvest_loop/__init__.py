"""Harvest Loop — Autonomous Farm Inspection Planning Platform.

This package is being built up one phase at a time. Phase 00 (this one)
only contains `graph.py` (a tiny two-node LangGraph "hello world") and
`llm.py` (a helper to talk to a local Ollama model). The `agents/`,
`data/`, `eval/`, `serving/`, and `security/` subpackages are placeholders
for later phases — see the Harvest Loop plan for what lands in each.
"""
