# Copy this to .env if you want to override the defaults.
# (llm.py reads these; if unset, it falls back to the values baked in.)

OLLAMA_BASE_URL=http://localhost:11434
HARVEST_LOOP_MODEL=qwen2.5:1.5b
